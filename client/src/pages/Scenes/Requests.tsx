import { Text, Flex } from '@chakra-ui/react';

const Requests: React.FC = () => (
  <Flex alignItems="center" width="100%" justifyContent="center" height="100%">
    <Text>إدارة الطلبات</Text>
  </Flex>
);

export default Requests;
